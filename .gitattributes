# Auto detect text files and perform LF normalization
# eol=lf forces LF in every working tree (including Windows CI runners where
# core.autocrlf would otherwise produce CRLF, which oxfmt rejects).
* text=auto eol=lf
